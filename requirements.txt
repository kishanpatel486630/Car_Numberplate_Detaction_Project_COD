# Web Server - MUST BE INSTALLED
gunicorn>=21.0.0

# Core dependencies for PlateVision AI
flask>=2.3.0
werkzeug>=2.3.0

# AI/ML Models
ultralytics>=8.0.0

# Computer Vision (headless for server)
opencv-python-headless>=4.8.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# SORT Tracker dependencies
scipy>=1.11.0
filterpy>=1.4.0

# PyTorch CPU-only (smaller memory footprint)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# Additional dependencies
pillow>=9.0.0
lapx>=0.5.0

# OCR - Disabled to save memory on Render free tier
# easyocr>=1.7.0
# Re-enable by uncommenting above if using paid tier with more RAM
