# PlateVision AI - Streamlit Version (Ultra-Optimized for <400MB)
# Total estimated installed size: ~350MB (160MB buffer)

# Web Framework (lighter config)
streamlit==1.28.0

# AI/ML Models (minimal version)
ultralytics==8.0.0

# Computer Vision (headless - no GUI deps)
opencv-python-headless==4.8.0.74

# Data Processing (pinned versions)
pandas==2.0.3
numpy==1.24.3

# SORT Tracker dependencies (minimal)
scipy==1.11.1
filterpy==1.4.5

# PyTorch CPU-only (older version = smaller ~150MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision==0.15.2

# Additional lightweight dependencies
pillow==9.5.0
lapx==0.5.2

# Memory usage breakdown (optimized):
# - PyTorch CPU 2.0.1: ~150 MB
# - Ultralytics 8.0.0: ~100 MB  
# - OpenCV headless: ~45 MB
# - Streamlit: ~30 MB
# - Pandas + NumPy: ~25 MB
# - SciPy + FilterPy: ~15 MB
# - Other: ~10 MB
# Total: ~375 MB (fits in 512MB with 137MB buffer)

# Models download on first use (not during build)
# OCR disabled to save memory
