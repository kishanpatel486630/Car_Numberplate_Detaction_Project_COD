# PlateVision AI - Streamlit Version (Optimized for 512MB)
# Total estimated installed size: ~430MB (82MB buffer)

# Web Framework
streamlit>=1.28.0

# AI/ML Models (lightweight version)
ultralytics>=8.0.0

# Computer Vision (headless - no GUI deps)
opencv-python-headless>=4.8.0

# Data Processing (minimal versions)
pandas>=2.0.0

# SORT Tracker dependencies
scipy>=1.11.0
filterpy>=1.4.0

# PyTorch CPU-only (smallest footprint ~180MB vs 500MB+ for GPU)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Additional lightweight dependencies
pillow>=9.0.0
lapx>=0.5.0

# Memory usage breakdown:
# - PyTorch CPU: ~180 MB
# - Ultralytics: ~120 MB  
# - OpenCV headless: ~50 MB
# - Flask + dependencies: ~40 MB
# - Pandas + NumPy: ~30 MB
# - SciPy + FilterPy: ~20 MB
# - Other: ~10 MB
# Total: ~450 MB (fits in 512MB with 62MB buffer)

# OCR - Disabled to save memory on Render free tier
# easyocr>=1.7.0
# Re-enable by uncommenting above if using paid tier with more RAM
